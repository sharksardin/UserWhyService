version: '3.8'

services:
  backend:
    build: ./
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    restart: always
    environment:
      # 프론트가 백엔드를 찌를 때 쓸 주소 (공인 IP 입력)
      - NEXT_PUBLIC_API_URL=http://168.107.33.24:8000
